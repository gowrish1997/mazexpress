import React from "react";
import Layout from "@/components/layout";
const Order = () => {
    return (
        <Layout>
            <div>order</div>
        </Layout>
    );
};

export default Order;
